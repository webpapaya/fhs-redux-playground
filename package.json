{
  "name": "kompott",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "docker-compose down -v && docker-compose up -d && sleep 1 && chokidar  \"**/*.sql\" -c \" docker-compose run flyway -url=jdbc:postgresql://db:5432/compup -user=dbuser -password=password migrate && docker-compose restart server\"",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "chokidar-cli": "^1.2.1"
  },
  "devDependencies": {
    "@babel/core": "^7.1.6",
    "@babel/plugin-proposal-class-properties": "^7.1.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/preset-react": "^7.0.0",
    "@babel/preset-stage-0": "^7.0.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "babel-loader": "^8.0.4",
    "babel-preset-env": "^1.7.0",
    "hamjest": "^3.2.1",
    "jest": "^23.6.0",
    "node-fetch": "^2.3.0"
  }
}
